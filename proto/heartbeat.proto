syntax = "proto3";
option go_package = "./proto";

message LeaderDetails {
  string NodeName = 1;
}
message ClientConfirmation {
  int32 Term = 1;
}

service HeartbeatService{
  rpc ReceiveHeartbeat(LeaderDetails) returns (ClientConfirmation);
}
