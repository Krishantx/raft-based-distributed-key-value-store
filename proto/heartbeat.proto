syntax = "proto3";
option go_package = "./proto";

message Leader {
  string NodeName = 1;
}
message ClientConfirmation {
  int32 Term = 1;
}

service HeartbeatService{
  rpc ReceiveHeartbeat(Leader) returns (ClientConfirmation);
}
